# WatermarkLogo.tsx

Static, prop-less decorative SVG: a translucent isometric cube watermark
layered into the dial knob's face. No behavior — purely visual, extracted so
the knob's markup isn't dominated by four multi-attribute `<polygon>`s.
