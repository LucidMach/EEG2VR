# DialReadout.tsx

Static-orientation overlay showing the current focus percentage and running
average, centered on the dial regardless of knob rotation.

**Props**: `phase`, `focus?`, `focus_avg?`. Shows `--` when `focus`/`focus_avg`
is `undefined`/`null`.
