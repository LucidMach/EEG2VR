# regionColors.ts

`REGION_RGBA` — maps each `ElectrodeMetadata.region` name (Frontal, Temporal,
Central, Parietal, Occipital) to an RGB triple used for that region's
waveform stroke color in [[drawElectrodeLane]].
