# DemoBottomControls.tsx

Demo Mode's floating bottom controls: [[../TrialProgressBar/index|TrialProgressBar]]
(the connected 40-trial timeline) and [[../TrialDial/index|TrialDial]] (the
rotary dial), both driven off the same `frame`/`onTrialSelect`.

**Props**: `frame` (`Frame`), `onTrialSelect(index, startOffset?)`.
