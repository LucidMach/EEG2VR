# AudioErrorToast.tsx

Prop-less toaster notification rendered by [[index]] whenever
`engine.audioError` is true (a demo trial's `/audio/video-NN.m4a` failed to
load — see [[../../hooks/usePlaybackEngine/useAudioSync|useAudioSync]]).
