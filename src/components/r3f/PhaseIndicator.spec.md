# PhaseIndicator.tsx

Centered pill in the HUD nav bar naming the current DEAP phase (Baseline /
Stimulus). Renders an empty positioned wrapper (for layout stability) outside
Demo Mode.

**Props**: `isDemo`, `phase` (`Frame["phase"]`).
