# PlaybackControls.tsx

Top-right HUD cluster: play/pause toggle (with a "Space" keyboard-hint
tooltip on hover) and the 1x/10x speed switch. Colors follow `phase`
(indigo for baseline, emerald for stimulus).

**Props**: `phase`, `isPaused`, `speed`, `onTogglePlayPause`, `onSetSpeed`.
